# Yusi Agent 产品与工程演进计划

> **Status:** In Progress — 面向 v1.0 上线闭环推进
> **Date:** 2026-08-04
> **Last Updated:** 2026-08-17
> **Scope:** 把 Yusi 建成完整的、逻辑闭环的、可上线的个人产品，并完成上线前准备

## 目标

本计划的目标是把 Yusi 建成一个**完整的、逻辑闭环的、可上线的个人产品**：核心价值回路
全程可用，每个认知与决策环节都有真实消费和质量证据，上线所需的安全、可观测与
可恢复能力准备就绪。核心价值回路：

```text
日记 / 对话 / 广场
        -> 认知与记忆
        -> 洞察、回顾与陪伴
        -> 灵魂匹配
        -> 匿名互动与连接反馈
        -> 改善下一次理解与匹配
```

不属于该定义的扩展功能（主动陪伴深度改造、持久化 Agentic Runtime、多 Agent、语音、
外部连接器、微调等）统一移入 [上线后扩展 Backlog](2026-08-17-yusi-post-release-expansion-backlog.md)，
上线达标前不启动。

本计划参考企业 Agent 路线图中的 Agent Runtime、工具治理、评测和反馈闭环，但不复制企业协作助手的渠道、多租户和 Agent Swarm 方案。

推进原则：**先完成闭环，再完成上线准备，最后才扩展体验**。即先补齐认知决策闭环与
质量门槛，再完成上线工程准备和上线验收；期间不新增用户可见功能。优先级从高到低：
逻辑闭环补齐、可重复评测与回归门槛、日志安全与可观测性、备份恢复与上线运维。

## 当前基础

Yusi 已具备以下基础能力：

- 日记、AI 对话和灵魂广场进入统一认知编排。
- `lifeGraph`、`user-persona`、`mid-memory` 形成分层认知结构。
- 匹配画像由认知层聚合，并通过 Dense + Sparse + RRF 召回和 LLM 精排生成匹配结果。
- Agent 已支持人格、主动问候、周期性回顾、反馈和多模态输入。
- 本地工具、MCP 工具、模型路由和外部 MCP 网关已经存在。
- 情景室可以独立完成多人情景、提交和 AI 报告生成。

当前的主要问题不再是缺少基础设施，而是上线闭环存在以下缺口：

1. LifeGraph 的 `importance` 写入链路完整，但决策链路不消费它：实体排序仍以
   `mentionCount` 主导，重要性没有形成“抽取 -> 决策 -> 评测”闭环；固定样例抽取回放
   基线尚未建立。
2. Phase 2 事件关联、Phase 3 的 Trace、超时、取消、重试和幂等账本均已落地；仅剩
   写操作用户确认这一防御性边界。持久化 Agentic Runtime 因无真实消费方移入扩展 Backlog。
3. Phase 4 评测已建立全部回放基线：LifeGraph promotion、中期记忆生命周期、
   Timeline、Timeline 重建、对话协议与匹配连接共 8 个套件 133 断言，另有统一
   汇总门槛（`quality-gates-aggregate-v1`）锁定套件合同与诚实边界；剩余缺口为
   真实模型语义评分与 `match.viewed` 事件（均以契约字段显式标记不可用）。
4. 日志安全已收敛：四个搜索组件 query 明文、十一处相邻正文命中与七条延期异常
   政策项均已处理（`f7b6cd4`、`1af6324`），由 `SensitiveLogSourceAuditTest`
   静态锁定；残余为范围外的既有技术异常日志与两处客户端响应体 message
   （`GlobalExceptionHandler` 响应契约、MCP gRPC `errorMessage`），均已归类
   待后续切片。
5. 上线工程准备缺失：无统一健康检查与指标暴露（未引入 actuator），无告警通道，
   数据备份与恢复未演练，无上线清单（灰度、回滚、降级）。
6. 限流与成本准入已存在（注解式 `@RateLimiter`、`model.gateway.admission` Redis 固定
   窗口预算），但未按上线标准复核覆盖面与阈值。

## 上线定义

“完整的、逻辑闭环的、可上线的个人产品”指同时满足以下四层，缺一不可：

1. **产品闭环完整**：用户可完成“日记/对话/广场表达 -> 记忆与认知形成 -> 记忆中心
   查看、纠正、删除 -> 洞察、回顾与陪伴 -> 灵魂匹配 -> 连接互动与反馈”的完整旅程，
   任何一环没有死链。
2. **逻辑闭环成立**：每个认知字段都有写入和决策消费（如 `importance` 参与排序与画像）；
   删除、隐藏、授权在所有读取路径真实生效；抽取、晋升、撤销有回放证据。
3. **质量证据可复现**：对话、记忆、Timeline、匹配的关键边界有固定样例回放和回归
   门槛，模型、Prompt、检索策略变更可与基线对比。
4. **上线准备就绪**：日志无敏感正文；服务与依赖健康可观测、关键失败可告警；备份
   已演练；账号与数据删除全路径生效；上线可灰度、可回滚。

四层全部具备证据后才进行首次上线；上线后只做修复与调优，扩展功能从 Backlog 按序取回。

## 推进记录

### 2026-08-04：完成事件关联 ID 盘点

已盘点聊天记忆、日记认知摄取、Embedding、LifeGraph、认知冲突、匹配、连接反馈、
灵魂聊天、通知和情景室边界。现有系统大多有记录主键；本次已为日记变更触发的异步
任务补上来源事件 ID。当时匹配推荐还没有独立的连接 ID，后续连接生命周期实现已补上该边界。

记录见：[事件关联 ID 盘点](../records/2026-08-04-yusi-event-association-inventory.md)。

本次确定的第一处代码改动是为 `DiaryChangedEvent` 生成 `eventId`，并将它作为
`triggerEventId` 传入 Embedding 和 LifeGraph 任务；不回填历史任务，不新建通用事件平台。

### 2026-08-04：完成聊天 AgentRun 生命周期摘要

已增加服务端 `agent_run_trace`，记录聊天 AgentRun 的启动、最近阶段、工具完成次数、
耗时以及完成、失败、取消终态。Trace 只保存低敏运行元数据，写入失败不会阻断聊天流。
详细的模型、Prompt、记忆召回和工具 Schema 仍留在后续 Trace 阶段。

### 2026-08-04：完成 AgentRun 对话流可视化

本次实现将用户从发送消息到最终回答的这一条 `/api/ai/chat/stream` 请求视为
一次 `AgentRun`。现阶段复用前端生成的 `requestId` 作为 `runId`，不把单次模型调用、
单个工具调用或 MCP 调用拆成新的 AgentRun：

```text
一次用户任务
  -> 一个 AgentRun（runId = requestId）
      -> 模型调用
      -> 记忆检索
      -> 本地 ToolCall / MCP ToolCall
      -> 最终回答
```

已完成的交付物：

- [x] 后端通过命名 SSE 事件发送 `run.started`、`run.stage`、`tool.started`、
      `tool.completed`、`response.delta`、`run.completed` 和 `run.failed`。
- [x] 阶段覆盖准备、思考、记忆检索、工具调用、回答生成、完成和失败。
- [x] 工具事件只包含调用 ID、工具名、来源、成功状态和耗时；不发送原始思考、
      工具参数或工具结果正文。
- [x] 前端在助手消息中展示 AgentRun 活动面板，并支持工具活动展开/收起。
- [x] 本地工具与 MCP 工具使用不同来源标识；当前 `web_search` 显示为 MCP。
- [x] 普通回答、工具完成、工具失败、连接异常和用户主动停止均有明确的显示状态。
- [x] 保留旧版纯文本 SSE 的解析兼容性。
- [x] 增加工具生命周期事件测试，验证 MCP 工具失败、耗时和敏感内容不外泄。
- [x] 将 `frontend/rebuild.sh` 对齐根目录部署脚本：支持严格失败、可选环境文件、无缓存构建，
      并只回收当前前端镜像，不清理全局 Docker 资源。

前端显示契约如下：

| 情况 | 用户看到的内容 |
| --- | --- |
| 普通回答进行中 | “正在整理问题”或“正在组织回答” |
| 记忆或人生图谱检索 | 友好的检索阶段文案 |
| 本地工具调用 | 工具友好名称、进行中/完成/失败、耗时 |
| MCP 工具调用 | 工具友好名称、`MCP` 来源标识、状态和耗时 |
| 普通回答完成 | 隐藏活动面板，只保留回答正文 |
| 使用工具后完成 | 保留可收起的工具活动记录和工具数量 |
| 工具或模型失败 | 保留失败状态和已生成的回答内容 |
| 用户主动停止 | 显示“已停止”，保留停止前已经生成的内容 |

本次没有提前实现的内容：

- AgentRun 事件目前是流式用户界面事件，还不是持久化 Trace。
- 尚未记录 Prompt 版本、模型调用 Token、费用、完整工具 Schema、重试和幂等信息。
- MCP 仍然只是 AgentRun 内的一种 ToolCall；外部 AI 调用 Yusi MCP 时，完整
  AgentRun 仍属于外部 AI，Yusi 侧只记录一次 MCP invocation。
- 情景室仍是独立小游戏，不自动进入本次 AgentRun 的长期画像或匹配画像。

验证结果：

- 后端 `AiControllerCancellationTest`、`ChatStreamCancellationRegistryTest`：9 个测试通过。
- 前端 TypeScript 检查、Vitest：4 个测试通过。
- 前端 Vite 生产构建通过。
- 本地开发服务可通过 `http://localhost:5174/` 访问。

### 2026-08-04：完成记忆透明度与生命周期切片

本次完成 Phase 1 的 `MidTermMemory`、`UserPersona` 和现有 `LifeGraph` 关系图谱实体
生命周期控制。用户可以从记忆中心查看安全摘要、来源元数据、更新时间、有效期、置信度
和匹配使用范围，并执行纠正、有效期调整、隐藏/恢复、删除和匹配授权。

实现记录见：[记忆透明度与生命周期](../records/2026-08-04-yusi-memory-transparency-lifecycle.md)。

本阶段的产品边界、工程设计和实现计划见：[记忆透明度与生命周期 PRD](../../prd/2026-08-04-memory-transparency-lifecycle.md)、
[工程设计](../../superpowers/specs/2026-08-04-memory-transparency-long-term-design.md)和
[实现计划](../../superpowers/plans/2026-08-04-long-term-memory-transparency.md)。

- 新生成的中期记忆默认 `matchAllowed=false`，用户显式开启后才参与匹配画像；历史数据迁移保留原有匹配行为。
- 隐藏、过期、已合并和未授权的记忆不进入匹配画像；隐藏和过期记忆也不进入聊天上下文、主动问候和记忆检索。
- 新生成的 Persona 和关系图谱实体默认不参与匹配；隐藏或过期数据从 Agent、洞察、图谱和匹配读取路径排除。
- 记忆中心、关系图谱搜索和 Agent 图谱工具只展示安全摘要和低敏元数据，不返回原始日记、完整对话或 mention 原文片段。
- `/community` 是唯一关系图谱前端入口；`/lifegraph` 已移除且不做兼容重定向。
- 记忆中心统一承接中期记忆、稳定画像和关系图谱生命周期控制，但不新增统一洞察 Hub。
- 修改摘要、匹配授权、隐藏状态或有效期后，必要时刷新匹配画像；向量副本同步失败不会阻断数据库生命周期操作。
- 日记编辑不强制回收全部历史派生记忆，用户通过记忆中心自行微调，避免把日记存储与派生认知强耦合。

### 2026-08-05：完成匹配到连接的第一版生命周期闭环

本次完成连接生命周期第一版，保留 `SoulMatch` 作为一次推荐记录，并新增独立的
`SoulConnection` 作为接受推荐后的连接生命周期记录。一个 `matchId` 当前最多对应一个
`connectionId`，避免把推荐、聊天和连接结果混成同一条记录。

- 接受、拒绝、等待回复、开始互动、双向共鸣、结束、举报和拉黑均有明确状态；双方都提交
  `DEEP_INTERACTION` 后才进入 `MUTUAL_RESONANCE`。
- 匹配响应返回 `connectionId` 和 `connectionStatus`；连接反馈、结束、举报和拉黑接口均按
  `matchId` 定位连接并记录独立 `connectionId`。
- 只有 `STARTED` 或 `MUTUAL_RESONANCE` 连接允许发送聊天消息；`REPORTED` 暂停普通互动，
  `DECLINED`、`ENDED` 和 `BLOCKED` 不允许静默恢复。
- `LIKE`、`DEEP_INTERACTION`、`DO_NOT_CONTINUE`、举报和拉黑作为不同反馈信号记录，强负面
  信号进入后续匹配排除逻辑。
- 数据库迁移为已有已接受匹配回填连接记录和 `match_feedback.connection_id`；旧的已匹配聊天
  在缺少迁移连接记录时保留兼容读取兜底。
- 匹配页已提供轻量连接反馈、结束、举报和拉黑入口；破冰话题与情景推荐仍是可跳过的辅助引导。

本次仍未完成连接产品事件 ID 的统一落库、消息行上的独立 `connectionId`/`runId`，以及完整的
通知和安全审计事件链；这些属于 Phase 2 后续工作。

### 2026-08-13：完成 LifeGraph 来源与升级边界落地

LifeGraph 已完成 Diary / 用户本人 Plaza 来源的证据追踪、来源替换、来源撤销、语义方向
和严格升级策略。抽取范围保持宽，但只有与用户本人或已确认重要人物存在明确生活语义
关系且有证据的内容才进入长期图谱；自动升级的一跳边界不限制 GraphRAG 查询多跳。

实现细节见：[LifeGraph 生活关系图与独立来源追踪设计](../../superpowers/specs/2026-08-12-lifegraph-source-provenance-design.md)。
历史无法还原来源的旧数据清理不在本次范围内。

### 2026-08-14：完成连接生命周期产品事件落库

连接状态发生真实变化时，现在会在同一事务中持久化服务端生成的产品事件 ID、稳定事件名、
事件结构版本、`connectionId`、`matchId`、操作者、前后状态、动作、低敏原因类别和发生时间。
连接当前状态继续保存在 `SoulConnection`，事件表额外保留不可替代的状态变更历史；重复提交
不会为无状态变化的操作重复生成事件。历史连接没有可靠的操作者和事件上下文，因此不做事件
历史回填。

本次只完成连接生命周期事件本身；推荐生成、通知、聊天消息和安全审计的跨表关联，以及后台
任务状态和恢复边界，继续作为 Phase 2 后续工作。

### 2026-08-14：完成 Phase 2 后端基础设施

Phase 2 的五个后端切片已完成并分别提交：产品记录跨流程关联、产品事件幂等账本、任务执行
账本、来源版本与派生数据幂等，以及安全审计轨迹。它们保持业务表为事实来源，不引入通用
事件溯源平台；产品事件、任务执行和安全审计分别承担业务事实、后台状态和安全追责职责。

- 连接、通知、聊天和匹配结果可以沿 `matchId`、`connectionId`、`runId` 和事件 ID 关联。
- Diary / Plaza 派生任务以用户、来源、来源版本和任务类型组成幂等边界，旧版本结果不能覆盖新版本。
- 任务失败、重试耗尽和恢复状态进入任务执行账本，审计只保存低敏动作、主体、资源、结果和分类 metadata。
- 安全审计按用户/参与者 scope 查询，管理员查询需要显式授权，过期记录先删除 scope 再删除事件。
- 后续 Phase 3 可以直接复用这些关联和安全边界扩展 AgentRun、模型、Prompt、记忆检索和工具 Trace，
  但不把用户可见功能提前到基础设施尚未验证之前。

### 2026-08-14：完成模型调用与 Prompt 版本 Trace 切片

本次完成 Phase 3 的模型调用可观测性基础：`PromptManager` 提供不可变的
`PromptSnapshot`，模型调用 Trace 保存 Prompt 的 key、version 和 locale，并支持管理端查询和筛选。
Trace 不保存 Prompt 正文、插值后的完整请求、用户输入、模型输出或思考内容。

- 直接调用模型的认知摄取、LifeGraph、图片理解、记忆压缩/融合、匹配、周报、主动问候、
  情绪分析和冲突检测链路，使用同一快照的模板和身份字段。
- Prompt 热更新、降级和旧版 `getPrompt(...)` API 保持兼容；没有显式快照的旧调用不会由
  Trace 层根据 `scene` 猜测版本。
- 本切片只完成模型调用与直接工作流的 Prompt 关联；聊天 AiServices 的动态系统消息、
  记忆召回、工具 Schema、跨任务 AgentRun 和异步恢复继续留在 Phase 3 后续切片。

### 2026-08-16：完成用户级后台 AgentRun 关联

本次完成 Phase 3 的后台运行关联切片。认知摄取、Diary / Plaza LifeGraph、周报和主动问候
现在各自拥有用户级 `TaskExecution` 与 `AgentRunTrace`，模型调用通过嵌套运行上下文继承同一
`runId`。运行记录只保存低敏生命周期字段，不保存原文、Prompt 正文、模型输出、思考内容、
工具参数或密钥。

- 认知摄取按用户、来源和来源版本幂等；空 Diary 仍清理旧中期记忆，但不触发模型调用。
- LifeGraph 任务沿用来源版本、删除和重试边界；重试等待保持 AgentRun 运行，只有成功或重试耗尽
  才进入终态。
- 周报使用用户与周期起始日生成稳定幂等键，`SoulReport.generationRunId` 保存用户级运行 ID；
  扫描批次 ID 只用于日志。
- 主动问候增加每日幂等任务和用户级运行；原有不活跃、冷却、频率和静默时段规则保持不变，
  模型失败可使用模板回退，通知失败才使用户级工作流失败。
- `ModelCallTrace` 支持按 `userId + runId` 查询，并增加对应联合索引；跨用户匹配批处理继续
  使用批次运行语义，不自动升级为用户级 AgentRun。

本次没有提前扩展新的用户流程。记忆召回摘要、工具 Schema/权限治理、取消传播、暂停恢复和
固定样例评测仍属于 Phase 3/4 后续工作。

### 2026-08-16：完成聊天工具 Trace 与依赖兼容刷新

本次完成 Phase 3 的聊天工具调用低敏 Trace 切片。工具调用作为 `AgentRun` 的持久化子记录，
保存本地生成的稳定 `toolCallId`、工具名称、来源、生命周期、耗时和固定失败分类；不保存查询、
参数、召回正文、工具结果、Prompt、模型输出、思考内容或密钥。

- `beforeToolExecution` 始终由后端生成本地唯一 ID；LangChain4j/MCP 的上游请求 ID 只作为可空的
  `upstreamToolCallId` 参考字段，不能承担幂等键职责。SSE 的开始和完成事件使用本地 ID。
- 工具调用通过请求对象 identity、上游 ID 和同名工具 FIFO 兜底关联；AgentRun 的 complete、fail、
  cancel 三个终态统一收敛同一运行下仍为 `RUNNING` 的工具 Trace，避免取消路径留下孤儿记录。
- `agent_run_trace.tool_count` 语义保持不变，仍表示已完成工具调用总数；`failureCategory` 只按固定
  枚举和 `hasFailed`/异常类型分类，不从工具结果文本或异常 message 推导原因。
- 本切片不覆盖 `McpGrpcServiceImpl` 中原始 `keyword/query` 日志，另立日志安全切片处理。
- Hutool 从 `5.8.24` 刷新到已核对的 `5.8.39`；Spring、LangChain4j、gRPC 等高耦合依赖保持不变。

本次没有提前扩展新的用户流程。工具能力 Schema/权限治理、取消传播、暂停恢复以及固定样例评测
仍属于 Phase 3/4 后续工作。

### 2026-08-16：完成 Agent 最终响应低敏摘要

本次完成聊天 AgentRun 的最终响应元数据切片。`agent_run_trace` 现在保存助手流式响应中已接收的
Unicode code point 数量；正常完成、模型失败和用户取消都会沿同一终态写入，因此失败或取消前
已经生成的部分回答也能被识别。数据库仍不保存响应正文、用户查询、Token 内容、Prompt 或工具结果。

- 响应数量只在聊天进程内从非空 `PartialResponse.text()` 累加，最终以 `response_char_count` 写入；
  它不是 Token 数、grapheme 数或可还原正文的字段。
- `AgentRunTraceService` 保留原有后台任务接口，非聊天运行默认记录 `0`；负数输入归零，终态更新
  继续幂等，不改变 `tool_count` 的“已完成工具调用总数”语义。
- `searchMemories`、`searchDiary` 和 `searchLifeGraph` 继续由 `AgentToolTrace` 记录调用生命周期，
  不新增内部检索明细表；记忆检索算法和 GraphRAG 查询深度不在本切片内调整。

本次没有提前扩展新的用户流程。工具能力 Schema/权限治理、取消传播、暂停恢复以及固定样例评测
仍属于 Phase 3/4 后续工作。

### 2026-08-16：完成工具能力契约切片

本次补齐 Phase 3 的第一层工具治理边界：本地工具从实际 `@Tool` 定义生成参数 Schema，MCP 工具
从服务端 `ToolSpecification` 动态登记；两类工具统一记录稳定名称、来源、能力版本、描述、权限范围
和参数 Schema。MCP 暴露给 LangChain4j 的规格会附带同一份版本和权限元数据。

- `AgentToolCapabilityCatalog` 只保存工具契约，不保存用户 query、参数值、工具结果或 Prompt；
  本地工具和 MCP 工具分别按来源隔离登记。
- `agent_tool_trace.capability_version` 记录一次实际调用使用的能力契约版本，使 run 回放可以解释
  当时使用的工具版本；旧 `AgentToolTraceService.start(...)` 调用保持兼容。
- 工具权限目前收敛为 `memory.read`、`persona.write`、`network.read` 和未知工具的
  `tool.execute`；能力版本变更需要显式升级版本号。
- `agent_run_trace.tool_count` 语义保持不变，仍表示已完成工具调用总数；本切片不引入超时、重试、
  幂等键或取消传播策略。

### 2026-08-16：完成工具执行超时与取消传播切片

本次在本地工具和 MCP 工具之间建立统一的执行策略边界：工具调用在专用线程池中执行，按能力
类型施加超时，并从聊天流 session 将用户取消传播到正在运行的工具。超时和取消只做一次终止尝试，
底层客户端是否响应线程中断仍由其自身实现决定；本切片不假设可以强制停止已经产生副作用的外部调用。

- `AgentToolExecutionPolicyService` 同时包装 `@Tool` 本地 executor 和动态 MCP `ToolProvider` 返回的
  executor，策略由工具能力目录统一提供；当前 memory read、persona write、MCP network read 和未知
  工具分别使用固定超时，线程池大小由 `agent.tool.execution.pool-size` 配置。
- `ChatStreamSession` 持有 `AgentCancellationToken`，取消或失败时先标记令牌，再取消流句柄；
  `ModelRouteContext` 和 `ModelRouteContextHolder.getEffective()` 将令牌传递到工具执行线程，避免
  session 从 registry 移除后丢失取消信号。
- 超时抛出固定的 `AgentToolTimeoutException`，取消抛出固定的 `AgentToolCancelledException`；
  不从异常 message 或工具结果推导失败类别，也不在本切片引入自动重试、幂等键或用户确认。
- `agent_run_trace.tool_count` 语义保持不变，仍表示已完成工具调用总数；`McpGrpcServiceImpl` 的
  原始 `keyword/query` 日志收敛仍属于独立安全日志切片。

本次没有提前扩展新的用户流程。工具重试、工具幂等键、写操作确认、暂停/恢复和固定样例评测仍属于
Phase 3/4 后续工作。

### 2026-08-16：完成工具超时重试与尝试计数切片

本次在统一工具执行包装层补充受能力契约约束的超时重试。抽取范围仍然保持宽，但能力目录只有明确
声明为 `READ/TIMEOUT_ONCE` 的只读工具允许一次超时重试；写操作、未知能力以及普通工具失败均不自动
重试。重试不扩展到 Controller、具体工具或 MCP 服务内部。

- 每次逻辑工具调用最多两次物理尝试，所有尝试共享不超过 30 秒的总 deadline，显著低于聊天 SSE 的
  180 秒生命周期；只有固定的 `AgentToolTimeoutException` 触发重试，`TOOL_FAILED`、`CANCELLED`、
  `UNKNOWN` 不重试。
- 重试退避使用可唤醒的取消令牌，等待期间收到取消会立即结束逻辑调用，不发起下一次尝试；
  取消或完成/失败/取消任一 run 终态都会清理未完成的请求身份注册。
- 专用线程池默认 8 个 daemon worker、16 个有限排队槽位，使用 `AbortPolicy`，不回退到调用线程。
  超时后不响应中断的旧尝试可能继续占用一个 worker，一次重试窗口最多同时占用旧尝试与新尝试两个
  槽位；本次不自动扩大池容量，也不创建第二个重试池。
- `agent_tool_trace.attempt_count` 只记录物理尝试数量，范围为 1 或 2，不保存参数、结果、Prompt、
  用户 query 或异常文本；`agent_run_trace.tool_count` 仍表示完成的逻辑工具调用数量，不按重试次数增加。
- 本次仍不实现通用幂等键、写操作用户确认、跨请求恢复，也不覆盖 `McpGrpcServiceImpl` 中原始
  `keyword/query` 日志收敛；失败分类继续只依据异常类型或 `hasFailed`。

### 2026-08-16：完成声明式写工具幂等账本与重复执行保护

本次完成 Phase 3 的声明式写工具保护切片。幂等账本复用 `agent_tool_trace`，不新增独立的
`idempotency_key`：Controller 生成的 `localToolCallId` 同时作为 `tool_call_id`、SSE 工具事件 ID、
Invocation Context 中的调用 ID 和账本 key。只有能力目录明确声明为 `WRITE + IDEMPOTENT_WRITE` 的工具
才进入账本；当前真实消费方只有本地 `updateUserPersona`，默认能力和未知工具仍拒绝自动重试及幂等写入。

- `updateUserPersona` 首次执行先原子 claim，Context 在专用 worker 中传播用户 ID 并被真实工具消费；同一
  本地调用 ID 的重放返回 `isError=true` 的稳定阻断响应，业务画像服务不会再次执行。
- 写工具没有自动超时重试；超时、取消、线程池提交失败和 run 终态收敛会把 `CLAIMED` 账本置为
  `UNKNOWN`，`UNKNOWN`、`COMPLETED`、`FAILED` 和并发 claim 失败都不会静默调用 delegate。启动扫描将
  超过 5 分钟的孤儿 `CLAIMED` 置为 `UNKNOWN`，账本状态保留 30 天。
- `AgentToolInvocationContext` 的显式跨线程传播已通过技术验证，不启用隐式 ThreadLocal 继承，也不需要
  `ModelRouteContextHolder` 退回方案。`agent_run_trace.tool_count` 仍表示完成的逻辑工具调用总数，不按
  claim 或物理尝试次数增加。
- 本次不覆盖 `McpGrpcServiceImpl` 中原始 `keyword/query` 日志，不增加用户确认、暂停/恢复或跨请求重放；
  这些边界另行切片处理。

### 后续推进顺序

按“先完成闭环，再完成上线准备，最后上线验收”推进，期间不扩展新的用户可见功能：

1. **认知逻辑闭环**：LifeGraph“固定抽取结果 + 真实 H2 promotion 回放”基线评测、
   `importance` 匹配画像决策消费、记忆中心人物关系投影（关系事实 + 确认来源 +
   importance 透明展示）均已完成；后续视基线结果评估人物字段改造是否独立切片。
2. **质量门槛收尾（已完成）**：对话（`chat-quality-v1`）、Timeline 重建
   （`lifegraph-timeline-rebuild-v1`）、匹配（`match-quality-v1`）评测集与统一
   回归门槛（`quality-gates-aggregate-v1`）均已落地。
3. **上线工程准备**：日志安全收敛、可观测与告警、备份恢复演练、安全与隐私自检、
   上线运维（见 Phase 5）。
4. **上线验收**：端到端验收与 GO/NO-GO 判定（见 Phase 6），随后首次上线。
5. 上线达标后，按需从 [上线后扩展 Backlog](2026-08-17-yusi-post-release-expansion-backlog.md) 取回条目。

## 设计决策

### 1. 产品优先于平台扩张

Yusi 的核心产品是“隐私优先的长期记忆 AI companion”，不是通用企业办公助手。后续优先提高理解质量、陪伴质量、连接质量和用户信任，不优先建设企业 IM 适配、多租户计费或工具市场。

### 2. 接受自然数据积累

用户没有足够日记、对话或互动数据时，Agent 无法形成深度理解是正常状态。项目不通过强制 onboarding、奖励机制、连续打卡或留存指标人为填充数据。

早期数据少时，系统可以保持克制：提供有限的当前上下文理解，不伪造稳定画像，也不为了让用户留下而过度主动打扰。产品质量主要看已有数据被使用得是否准确、透明和安全，而不是新用户是否被快速转化或长期留存。

### 3. 保持一个 Yusi Agent，按能力扩展

现阶段采用“一个主 Agent + 认知、记忆、匹配、报告等领域能力”的结构。只有当任务出现明确的多步骤协作需求时，才引入 Supervisor 或专业 Agent，不为了架构形式强行拆分多个 Agent。

### 4. Agentic 只用于长流程和确认流程

普通聊天、认知摄取、匹配画像刷新和周报生成继续由现有 Spring Service 和异步任务承载。需要计划、等待、用户确认、恢复或补偿的流程，再引入持久化 Agent Run，例如：

- 用户请求删除一组长期记忆。
- Agent 整理一段较长的人生阶段记录。
- 用户确认后才把一次情景结果用于匹配。

### 5. 先做评测，再考虑训练

当前不建设完整的微调或训练工厂。先收集可复现的对话、记忆和匹配评测样例，建立 Prompt、模型和检索策略的回放比较能力。没有可靠评测之前，训练不会自动带来可靠提升。

### 6. 上线闭环优先，体验扩展靠后

上线定义只包含让现有核心价值回路真实可用所需的一切；主动陪伴深度改造、持久化
Agentic Runtime、多 Agent 编排、语音与外部连接器等体验扩展，一律在上线标准达成后
从扩展 Backlog 取回，避免功能发散稀释上线准备。

## 范围与非目标

### 本计划范围（上线定义内）

- 认知决策闭环：`importance` 决策消费、抽取与晋升回放基线。
- 记忆的透明度、权限和生命周期（已完成，维持回归）。
- 匹配后的连接闭环和安全反馈（已完成，维持回归）。
- Agent Run、工具调用和模型调用的可观测性（剩余写操作确认防御性边界）。
- 对话、记忆、Timeline 和匹配的离线评测与回归门槛。
- 上线工程准备：日志安全、可观测与告警、备份恢复、安全隐私自检、上线运维。

### 移入扩展 Backlog（上线达标后按需取回）

- 主动陪伴深度改造（活动聚合、触发类型拆分、触达时机）。
- 持久化 Agentic Runtime（等待确认流程出现真实消费者）。
- Supervisor 多 Agent、语音对话、外部知识接入、个人数据连接器、MCP 工具生态。
- 微调与专用模型、用户反馈转脱敏标注数据。

详见 [上线后扩展 Backlog](2026-08-17-yusi-post-release-expansion-backlog.md)。

### 长期非目标

- 新用户冷启动、激活漏斗和留存优化。
- 强制 onboarding、连续打卡或奖励式数据收集。
- 飞书、Slack、钉钉等企业渠道适配。
- 企业多租户、组织权限、计费和配额平台。
- 完整的四专家 Agent Swarm。
- 将情景室自动写入长期画像或自动影响所有匹配。

## 阶段计划

### Phase 0：产品契约与质量基线

**目标：** 在继续加功能前，明确什么叫“理解得更好”和“连接成功”。

- [x] 定义核心用户事件：首次表达、记忆形成、洞察查看、匹配接受、首次回复、持续互动、双向共鸣、结束连接、举报和遗忘。
- [x] 定义一次连接的状态模型和状态转换规则。
- [x] 定义对话质量、记忆质量、匹配质量和主动问候质量的基础指标。
- [x] 盘点现有事件、异步任务、反馈表和通知表，标记缺少的关联 ID。
- [x] 建立一批脱敏的基准案例，覆盖稀疏数据、记忆召回、认知冲突、匹配和主动问候。

**交付物：** [产品事件与评测基线](../specs/2026-08-04-yusi-agent-product-event-contract.md)、
[事件关联 ID 盘点](../records/2026-08-04-yusi-event-association-inventory.md)、连接状态模型、
质量指标说明和第一版评测样例。

### Phase 1：记忆信任与生命周期

**目标：** 让用户在数据自然积累后，能够理解、纠正和控制 Agent 对自己的认知。

- [x] 完成第一批记忆中心，展示 `MidTermMemory` 的近期状态摘要及其来源、生命周期和使用范围。
- [x] 为 `MidTermMemory` 增加来源、更新时间、有效期、置信度、隐藏状态和匹配授权。
- [x] 支持用户纠正摘要、调整有效期、隐藏/恢复、删除和要求遗忘单条中期记忆。
- [x] 将“是否允许参与匹配”作为独立的记忆使用范围；新生成记忆默认不开放。
- [x] 保持原始日记、认知摘要和匹配画像之间的边界，摘要以外的敏感原文不进入记忆中心。
- [x] 为 `UserPersona`、`LifeGraph` 和更长期的稳定结构补充同等透明度与生命周期控制。
- [x] 为 Diary / 用户本人 Plaza 的 LifeGraph 抽取增加独立来源证据、来源替换、来源撤销、
      语义方向和重要人物升级边界；历史旧数据清理不纳入本阶段。

**验收标准：**

- 用户能回答 Agent 记住了什么、来自哪里、是否参与匹配。
- 删除或隐藏记忆后，后续对话和匹配不会继续使用该记忆。
- 数据不足时，系统不会伪造稳定画像或为了留存强行生成结论。

### Phase 2：事件关联、数据一致性与安全审计

**目标：** 让用户可见的连接、通知和聊天结果，以及后台认知任务，都能被准确关联、
审计、重试和追责；本阶段优先建设后端边界，不增加新的用户流程。

**当前状态：** Phase 2 后端基础已完成；用户可见的连接流程保持现有行为，后续优先沿基础设施
继续完善 AgentRun、推荐生成、质量评测和主动性后端，不增加新的用户流程。

- [x] 明确匹配和连接的完整状态：推荐、接受、拒绝、等待回复、已开始、持续互动、
      双向共鸣、结束、举报和拉黑。
- [x] 区分“喜欢这次连接”“互动很深”“不安全”“不想继续”等反馈信号。
- [x] 完善举报、拉黑、结束/拒绝和聊天权限保护流程。
- [x] 为连接状态变化统一生成并持久化产品事件 ID。
- [x] 将推荐生成、连接状态、通知、聊天消息、反馈和安全审计关联到 `connectionId`、
      `matchId` 和必要的 `runId`。
- [x] 为事件定义用户、来源、操作者、发生时间、幂等键和安全敏感级别，禁止跨用户关联。
- [x] 为 Diary、Plaza、Embedding、LifeGraph、Persona、周报和匹配任务统一记录任务状态、
      触发事件、失败原因、重试次数和最后一次可恢复位置。
- [x] 明确来源替换、删除、重复事件和并发任务的幂等规则，并验证派生数据不会重复累加。
- [x] 建立安全审计事件的保留、查询、脱敏和访问边界；审计记录不保存原始敏感正文。

**验收标准：**

- 任一连接可以从推荐追踪到互动、反馈、通知和最终结果，且不会混入其他用户或其他连接。
- 同一业务事件重复投递不会重复写入状态、通知、记忆或审计记录。
- Diary / Plaza 修改、删除和失败重试不会留下无法解释的旧来源贡献。
- 关键后台任务可以查询当前状态、失败原因和下一次恢复动作。

### Phase 3：Agent 运行时、模型调用与任务可靠性

**目标：** 建立统一的后端运行记录和工具治理，让聊天、认知摄取、匹配、报告和主动任务
都可以被观测和恢复；流式 UI 只作为安全投影。

**当前进度：** 用户聊天流的 AgentRun 事件层和低敏生命周期摘要、聊天工具 Trace、最终响应低敏
摘要、模型调用与 Prompt 身份 Trace、工具能力契约、工具执行超时、取消传播与受限超时重试，以及
认知摄取、LifeGraph、周报和主动问候的用户级后台运行关联已完成；Phase 2 已提供产品事件、任务执行
和安全审计关联。声明式写工具幂等已完成；仅剩写操作用户确认这一防御性边界；持久化
Agentic Runtime 因无真实消费方移入[上线后扩展 Backlog](2026-08-17-yusi-post-release-expansion-backlog.md)。

- [x] 为用户聊天流建立用户可见的 `AgentRun` 标识，使用 `requestId` 关联一次任务。
- [x] 为聊天流发送安全的阶段、工具生命周期、回答增量和终态事件。
- [x] 前端展示普通阶段、本地工具、MCP 工具、失败和取消状态，并保持流式回答体验。
- [x] 为聊天 AgentRun 持久化低敏生命周期摘要、工具次数、耗时和终态。
- [x] 为模型调用 Trace 记录模型、路由、耗时、Token、错误等低敏摘要，并关联实际使用的
      Prompt key、version 和 locale；Trace 不保存 Prompt 正文、原始输入、输出或思考内容。
- [x] 将 AgentRun 标识扩展到认知摄取、LifeGraph、周报和主动问候，并沿用户、来源、版本和任务
      幂等边界关联；跨用户匹配批处理明确保留批次运行语义，不纳入用户级 AgentRun。
- [x] 为聊天工具调用建立统一的低敏摘要，使用本地工具调用 ID 关联生命周期并在 AgentRun 终态
      收敛未完成记录；禁止持久化原始思考和敏感工具正文。
- [x] 为记忆召回工具和最终 Agent 结果建立统一的低敏摘要：记忆召回沿 `AgentToolTrace` 关联，
      最终结果只记录响应 code point 数量和既有终态；禁止持久化原始思考和敏感工具正文。
- [x] 为本地工具和 MCP 工具定义统一的能力描述、参数 Schema、权限范围和版本。
- [x] 为本地工具和 MCP 工具增加统一超时策略与聊天取消传播；执行上下文携带取消令牌，超时/取消
      由固定异常终止等待并尽力中断底层执行。
- [x] 为明确允许的只读工具增加最多一次超时自动重试、逻辑调用总 deadline、可取消退避和低敏尝试计数；
      不对写操作、未知能力或非超时失败自动重试。
- [x] 为能力目录明确声明的写工具增加与 `localToolCallId/tool_call_id` 共用的幂等 claim 账本；未知和
      未声明写工具默认拒绝重放，写操作用户确认仍单独保留为后续边界。
- [ ] 为需要用户确认的非幂等写操作建立确认、暂停和恢复边界。定位为防御性边界：
      当出现真实非幂等写工具时实现；若首次上线前始终没有出现，则保持现有
      声明式幂等账本即可，本项转为上线前验收时的明确检查点。
- [x] 为认知摄取、LifeGraph、Embedding、周报和主动问候等异步任务补充任务状态、失败原因和
      恢复策略；重试复用逻辑运行 ID，终态更新幂等。
- [ ] ~~只将需要暂停、确认和恢复的流程迁移到持久化 Agentic Runtime，不重写普通同步服务。~~
      已移入[上线后扩展 Backlog](2026-08-17-yusi-post-release-expansion-backlog.md)：
      当前确认流程无真实消费者，不属于上线定义范围。

**验收标准：**

- 能通过一个 `runId` 还原一次 Agent 工作经过了哪些步骤、使用了哪些版本和发生了什么错误。
- 单个模型或工具失败不会导致整个用户状态重复写入，重试具有明确幂等边界。
- 异步任务可以安全重试、取消或恢复，且不会绕过来源、权限和安全审计规则。
- 普通聊天仍保持现有流式体验，不被复杂任务编排拖慢。

### Phase 4：评测、回放与质量回归

**目标：** 在扩展用户可见功能前，建立可重复的质量证据和回归门槛，让 Prompt、模型、
抽取策略和检索策略的变化可比较。

- [x] Phase 4 第一刀：完成基于 H2 的 LifeGraph / Timeline 固定样例回放基线，覆盖
      `EVAL-MEM-002-A/B/C`、`EVAL-TIMELINE-001-A`、来源 revision、重复贡献、删除残留和
      Timeline 资格；报告接入默认 Maven 测试并由 CI 归档。

- [x] Phase 4 第二刀：完成 `EVAL-MEM-001-A/B/C` 中期记忆生命周期与消费边界回放，覆盖
      隐藏/过期/合并过滤、匹配画像授权、向量删除失败后的数据库与残留候选隔离、跨用户隔离；
      使用脱敏 fixture、真实 H2 与真实 `MatchProfile` 输出，报告接入默认 Maven 测试并由 CI 归档。

- [x] Phase 4 第三刀：完成 `EVAL-MEM-003-A/B/C` LifeGraph“固定抽取结果 + 真实 H2
      promotion 回放”基线，固化 `User -> 重要人物 -> 属性/事件`、证据要求、`0.6`
      置信度边界、`REJECTED_RELATIONS` 和禁止人物关系扩散的评测契约；人物字段
      改造不在此切片。
- [x] 将 `importance` 接入决策消费（实体排序、匹配画像优先级），先用回放基线对照现状，
      闭环验证通过后再评估人物重要性字段改造是否独立切片。
- [x] 记忆中心人物关系投影：Person 卡片只读展示与用户的直接关系类型、`AUTO/MANUAL`
      确认来源和 `importance`；关系事实每次读取从 `life_graph_relation` 当前行投影，
      不落地缓存字段，配套真实 H2 回放（`lifegraph-memory-relation-v1`）。

- [x] 建立对话评测集：记忆引用正确性、语气一致性、幻觉、隐私边界和工具使用边界。
      配套 `chat-quality-v1`（4 case、10 断言）；真实模型语义评分缺口以
      `semanticModelScoreAvailable=false` 显式标记。
- [ ] 建立记忆评测集：实体/关系抽取、LifeGraph 升级、来源撤销、去重、冲突识别、
      有效期和删除生效。
- [x] 建立 Timeline 评测集：事件资格、时间证据、来源安全性和修改/删除后的重建结果。
      配套 `lifegraph-timeline-v1` 与 `lifegraph-timeline-rebuild-v1`（五表残留全扫描）。
- [x] 建立匹配评测集：召回覆盖、精排理由、双向共鸣、接受率、持续互动和强负面排除。
      配套 `match-quality-v1`；生产缺 `match.viewed` 事件，接受率以
      `acceptanceRateAvailable=false`、`viewedCount=0` 诚实标记，禁止用推荐数冒充分母。
- [ ] ~~建立主动性评测集：触发是否合理、活动判断是否准确、内容是否重复、是否造成打扰。~~
      已移入[上线后扩展 Backlog](2026-08-17-yusi-post-release-expansion-backlog.md)：
      主动问候保持现有行为上线，评测深化随体验改造一起取回。
- [ ] 支持固定样例回放和 Prompt/模型/检索版本对比，记录输入版本、输出摘要和评测结果。
- [ ] ~~将用户反馈转成脱敏、可标注的数据，不直接把所有反馈写回画像或训练数据。~~
      已移入[上线后扩展 Backlog](2026-08-17-yusi-post-release-expansion-backlog.md)：
      属于训练与数据工厂方向，非上线必需。
- [x] 为关键指标设置回归门槛，任何质量提升都不能以隐私、安全、删除生效或来源正确性
      下降为代价。配套 `QualityGatePolicy`（SuiteContract 锁定 case 集合与最小断言数）
      与 `quality-gates-aggregate-v1` 汇总门槛（诚实边界 `AGGREGATE_HONEST_BOUNDARY`
      防缺口被悄悄补齐）。

**验收标准：**

- 修改模型、Prompt、升级策略或检索策略后，可以与基线进行固定样例对比。
- LifeGraph、Timeline、记忆删除和安全边界有自动化回归结果，而不是只依赖线上观察。
- 匹配调整在上线前有可解释的质量证据。

### Phase 5：上线工程准备

**目标：** 在首次上线前完成安全、可观测、可恢复与可运维准备；本阶段不新增用户可见功能。
主动问候保持现有不活跃判断、冷却、频率和静默时段行为，只作为上线验收对象，不做改造
（深度改造见扩展 Backlog）。

- [x] 日志安全收敛：`McpGrpcServiceImpl`、`DiarySearchTool`、`LifeGraphTool`、
      `MidTermMemorySearchService` 的日志去除用户 query 明文，统一为低敏格式（只保留
      用户 ID、数量、耗时和固定分类）；并对全量日志做一次敏感正文 grep 自检。
      实际超出条目范围：同步收敛十一处相邻正文命中与五类异常政策（含九处同类
      伴随日志），由 `SensitiveLogSourceAuditTest` 锁定直接 payload 零容忍与
      延期 allowlist 为空。
- [x] 引入统一健康检查与指标暴露（actuator / Micrometer），覆盖 HTTP、MySQL、Redis、
      Milvus、模型网关和关键后台任务（提交 f1335d8f：liveness/readiness 组契约、
      独立管理端口只暴露 health/prometheus 且 show-details=never、四标签白名单指标、
      traceId 五入口传播与复用前清理；配套日志安全已由 f7b6cd4/1af6324 完成。
      部署验收项待生产完成：管理端口网络隔离、Prometheus 真实抓取、真实依赖连通性）。
- [ ] 建立最小告警通道：服务不可用、模型调用失败率、后台任务积压和预算准入拒绝都有
      告警阈值和接收人（接收通道决策：优先采用飞书告警机器人，用户指定。前置事实
      更正：f1335d8f 实际暴露的可消费信号仅为 readiness 组、tool_search 系列与
      model_call_total/failure_total/latency；dependency_health、task_due_gap/task_lag、
      budget_denied_total 四项已随 e1435711 补齐，见 2026-08-20 告警通道设计文档；
      应用内 AlertEvaluator + FeishuAlertNotifier 与四类阈值规则、跨实例去重、低敏
      消息契约已落地（本地证据 mock-contract-only，默认开关关闭）；待完成：真实
      Secret 注入与飞书送达、双副本去重与生产阈值调优等 deployment-only 验收及
      轮值接收人确认）。
- [ ] 制定并演练数据备份与恢复：MySQL、Milvus、Redis 和对象存储都有备份周期与恢复步骤，
      至少完成一次真实恢复演练并记录 RTO。
- [ ] 安全与隐私自检：账号注销与数据删除全路径验证（含向量副本、派生认知与审计保留
      边界）、API 鉴权与越权回归、低敏 Trace 边界全量复查（注销删除协调器已由
      739e1ae7 落地：失败闭锁、三 collection/Redis 分族/OSS inventory 契约、去标识化
      审计与 orphan 门槛；本地证据为 application-invariant-only/mock-contract-only，
      真实 Milvus/Redis/OSS/worker/备份副本的 deployment-only 证据与 API 鉴权越权
      回归、Trace 边界复查两项子项待完成）。
- [ ] 限流与成本准入复核：`@RateLimiter` 场景覆盖面与 `model.gateway.admission` 阈值按
      上线标准复核，补齐无限流的写接口。（本地门控已就绪：c778137 交付 90 写映射
      覆盖 manifest、96 个注解（22 既有 + 74 新增）、HMAC subject fail-closed、固定
      低敏 429、bounded local fallback 与 admission 四分类一致性；验收发现的指标
      白名单对调缺陷已由 bef48267 修复并补 operation 标签值透传断言，全量 468
      tests 通过；本地证据为 application-invariant-only/mock-contract-only。待完成
      （deployment-only）：YUSI_RATE_LIMIT_HMAC_SECRET 生产注入（未注入时 subject
      维度限流 fail-closed，为硬性前置）、真实并发/SSE/Multipart 压测、网关字节与
      并发限流、Redis 多副本与故障演练、供应商配额校准与阈值调优、20611
      allowlist、WebSocket/gRPC 入口验证。）
- [ ] 上线运维准备：灰度与回滚方案、模型网关与关键依赖降级策略、上线清单与应急流程。
      （部署形态决策 2026-08-22：实际生产为单机 Compose 路径（build_deploy.yml 手动
      触发 + rebuild.sh force-recreate）；K8s/GitOps 路径（deploy_k8s.yml + yusi-infra，
      已验证可用）因服务器配置不足暂搁置；本轮不做灰度、不做多副本，回滚能力如实
      登记为当前单机路径不具备（旧镜像被清理、无切回命令）；上线运维设计 d51aa4e
      中的 K8s/灰度内容为搁置蓝图，待按单机维度修订；本条目实际范围收敛为：单机
      上线清单、依赖降级演练、飞书告警通道与应急流程、发布后观察窗口。）

**验收标准：**

- 日志中不再出现任何用户 query、记忆正文、Prompt 或工具结果。
- 一个可观测面板能回答服务及其依赖是否健康，关键失败能在无人值守时被告警。
- 删除一个用户的全部数据后，任何读取路径都查不到残留。
- 备份能在记录的 RTO 内完成恢复演练。

### Phase 6：上线验收与首次上线

**目标：** 用端到端验收和完整质量证据完成 GO/NO-GO 判定，并进行首次上线。

- [ ] 端到端验收：以新用户和老用户视角分别走通“日记/对话 -> 认知 -> 记忆管理 ->
      匹配 -> 连接与反馈”完整旅程，确认无死链。
- [ ] 关键路径性能与容量确认：聊天流、记忆检索、匹配批处理在目标并发下达标，模型
      预算准入在压力下保护系统不雪崩。
- [ ] 所有评测集在上线版本回归通过，报告归档并作为 GO/NO-GO 证据。
- [ ] 对照“上线标准”逐项确认证据齐全，完成 GO/NO-GO 评审。
- [ ] 首次上线采用灰度方式并保留快速回滚能力；上线后只修复缺陷和调整阈值，不新增
      功能，扩展需求从 Backlog 按序评审。

**验收标准：**

- 下文“上线标准”的每一条都有对应证据（评测报告、演练记录、自检报告）。
- 上线失败可以回滚，且回滚不依赖任何数据修复。

## 情景室边界

情景室继续作为独立的小游戏和探索性体验，不纳入日记、聊天、广场的自动认知摄取链路。

```text
情景室
  -> 参与情景
  -> 生成情景报告
  -> 用户主动选择是否带入一次对话或一次匹配
```

保留以下弱连接：

- 匹配后由 `ConnectionGuideService` 推荐适合双方的已审核情景。
- 用户主动点击“带入当前对话”或“用于本次匹配”时，才创建一次性上下文。
- 一次性上下文只传递必要的主题、诉求或状态标签，并设置有效期。
- 情景室继续使用独立的完成率、复玩率和满意度指标。

不做以下连接：

- 不自动把情景原文写入长期记忆。
- 不自动改变 `user-persona` 或 `MatchProfile`。
- 不因为用户玩过某个情景，就改变所有后续推荐。

## 关键风险与约束

- 记忆越强，隐私风险越高；任何长期记忆都必须有来源、范围和删除路径。
- 主动性越强，越容易产生打扰；默认低频，所有主动能力可关闭。
- 匹配互动越多不等于质量越高，必须加入双向反馈和安全信号。
- 多 Agent 会增加状态、成本、延迟和调试复杂度，必须由实际任务驱动。
- MCP 是开放能力边界，不应反过来成为核心业务数据的所有者。
- 训练不是当前第一优先级；先保证数据脱敏、评测可复现和质量回归。
- 上线准备本身也有顺序风险：可观测与备份必须先于首次上线完成，否则任何线上
  故障的代价都不可控；评测门槛未过不进入 Phase 5，Phase 5 未完成不进入上线验收。

## 上线标准

首次上线前，Yusi 必须同时满足：

1. 核心价值回路真实可用：用户能完成从日记/对话积累、到记忆查看与纠正删除、到
   匹配与连接反馈的完整旅程，任何一环没有死链。
2. 认知逻辑闭环成立：`importance` 参与决策，删除、隐藏与授权在所有读取路径生效，
   抽取、晋升与撤销有回放证据。
3. 用户清楚 Agent 记住了什么，并能管理这些记忆；数据不足时系统保持克制，不伪造
   稳定画像或强行制造互动。
4. 重要 Agent 运行可以追踪，模型和 Prompt 变化可以评测，关键边界有自动化回归。
5. 日志与 Trace 不含敏感正文；账号注销与数据删除全路径生效。
6. 服务健康可观测，关键失败可告警，备份已演练，上线可灰度、可回滚。
7. 情景室仍然是独立、轻量、可探索的小游戏，不破坏主流程。

以上标准之外的能力均属扩展项，见 [上线后扩展 Backlog](2026-08-17-yusi-post-release-expansion-backlog.md)。
