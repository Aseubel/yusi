package com.aseubel.yusi.controller;

import org.springframework.stereotype.Controller;

/**
 * WebSocket chat messages are sent through the authenticated REST endpoint;
 * this controller remains as a compatibility component for the old mapping.
 */
@Controller
public class WebSocketChatController {
}
