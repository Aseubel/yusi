1. 在提到某个文档或某处功能代码时，最好能直接引用。
2. 在修改完代码后执行对应构建 pnpm run build 或 ./mvnw test.
3. 在git commit或git push前先告知用户，让用户检查或执行可能的test，直到得到用户明确的指令。
4. 若commit或push被hook拦住后需要修改代码，修改代码后再执行一次构建并由用户确认。
5. 对于用户的业务要求，在修改执行前先确认是否有更好的最佳实践方案，如果有则向用户提出并确认下一步行动。
6. 不要自己启动应用服务器。
7. 非用户明确要求，不要开子agent，你可以在大型任务时提出建议，但不能自己执行